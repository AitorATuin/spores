package com.logikujo.Spores

import dispatch._

object Spores {
  def main(args:Array[String]):Unit = {
    //val github = Spore("https://raw.github.com/SPORE/api-description/master/services/github.json")
    //github("get_info")(Sequence("format" -> "json", "username" -> "schacon"))
  }

/*    println("JJJ")
    val jj = github("get_info")
    for (j <- jj) yield j match {
      case Left(v) => println("Error " + v)
      case Right(v) => println(v)
    }
  }*/
}
